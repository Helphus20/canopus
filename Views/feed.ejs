<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Canopus | Feed</title>
</head>
<body>

    <% if (error) { %>
        <div class="alert alert-danger" role="alert">
            <%= error %>
        </div>
    <% } %>

    <% if (success) { %>
        <div class="alert alert-success" role="alert">
            <%= success %>
        </div>
    <% } %>

    <div class="container-fluid">
        <div class="row">
            <!-- Painel à Esquerda -->
            <div class="col-md-3 bg-light p-3">
                <div class="d-grid gap-2 mb-4">
                    <!-- Botão Pesquisar Usuários -->
                    <button class="btn btn-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#searchContainer" aria-expanded="false" aria-controls="searchContainer">
                        Pesquisar Usuários
                    </button>
                    <div class="collapse" id="searchContainer">
                        <input type="text" class="form-control mt-2" id="searchInput" placeholder="Digite o nome do usuário">
                        <button class="btn btn-success mt-2" onclick="searchUser()">Buscar</button>
                    </div>
                    <a class="btn btn-primary" type="button" href="/newPost">Nova Postagem</a>
                    <form action="/logout" method="GET">
                        <button class="btn btn-danger" type="submit">Logout</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

            <!-- Seção Central para Postagens -->
            <div class="col-md-9 p-4">
                <h2 class="mb-5 mt-5">Seu Feed de Postagens</h2>
                <div class="postagem mb-3">
                    <div class="card">
                        <div class="card-body">
                            <p class="card-text">Aqui quero colocar a imagem</p>
                        </div>
                        <div class="card-body">
                            <p class="card-text">Aqui vai ser a legenda da postagem</p>
                        </div>
                    </div>
                </div>
                <!-- Adicione mais postagens conforme necessário -->
            </div>
        </div>
    </div>

    <script>
        function searchUser() {
            const userName = document.getElementById('searchInput').value;
            // Aqui você pode adicionar a lógica para buscar o usuário
            alert(`Buscando usuário: ${userName}`);
        }
    </script>
    
    <!-- Inclua os scripts do Bootstrap no final do seu arquivo HTML -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>